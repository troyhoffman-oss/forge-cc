# /forge:plan — Interview to Requirement Graph

> Stub — full skill will be written in Milestone 2.

Replaces `/forge:spec`. Runs adaptive interview and produces a requirement graph.

See `ref/requirement-sizing.md` for sizing rules and vertical slice enforcement.
