# /forge:build — Graph Execution with Adversarial Review

> Stub — full skill will be written in Milestone 3.

Replaces `/forge:go`. Orchestrates graph execution with adversarial review, worktree isolation, and Linear state transitions.

See `ref/adversarial-review.md` for review protocol, `ref/graph-correction.md` for mid-execution corrections.
